name: CLA check

# pull_request_target runs with a writable token so signatures can be committed.
# It is safe here because this workflow never checks out or executes pull
# request code — it only reads the event payload and writes the signature file.
on:
  issue_comment:
    types: [created]
  pull_request_target:
    types: [opened, closed, synchronize]

permissions: {}

concurrency:
  group: cla-${{ github.event.pull_request.number || github.event.issue.number || github.run_id }}
  cancel-in-progress: true

jobs:
  cla:
    name: CLA check
    if: >-
      github.event_name == 'pull_request_target' ||
      github.event.comment.body == 'recheck' ||
      github.event.comment.body == 'I have read the CLA Document and I hereby sign the CLA'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      actions: write # contributor-assistant updates its workflow state
      contents: write # signatures are committed to the signatures branch
      pull-requests: write # contributor-assistant comments on the PR
      statuses: write # CLA status check
    steps:
      - name: Check contributor agreement
        uses: contributor-assistant/github-action@ca4a40a7d1004f18d9960b404b97e5f30a505a08 # v2.6.1
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          path-to-signatures: signatures/version1/cla.json
          path-to-document: https://github.com/janiorvalle/hop/blob/main/CONTRIBUTING.md
          branch: signatures
          allowlist: dependabot[bot],*[bot]
